{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    // Optional: speed up build since we only emit app code
    "noEmit": false
  },
  "include": ["src"]
}